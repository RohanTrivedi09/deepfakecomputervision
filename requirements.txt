# Local demo/inference only — training runs separately in
# notebooks/deepfake_training.ipynb on Colab (that notebook installs its own
# deps in-session; see its markdown intro).

# Core
torch>=2.10
torchvision>=0.21
numpy>=1.26
pillow>=10.3

# Grad-CAM overlay rendering (src/inference/predict.py's hand-rolled GradCAM,
# mirroring the training notebook's implementation)
opencv-python-headless>=4.9

# App
streamlit>=1.38
